version: '3.8'

services:
  web:
    build: .
    expose:
      - "5001"
    volumes:
      - ./app:/app
    environment:
      - FLASK_ENV=development
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    deploy:
      replicas: 3
  redis:
    image: "redis:alpine"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web

volumes:
  redis-data: